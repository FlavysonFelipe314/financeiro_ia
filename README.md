clone o repositorio com:
git clone https://github.com/FlavysonFelipe314/financeiro_ia.git

crie o banco de dados com:
php artisan db:create

rode as migrations:
php artisan migrate

[OPCIONAL] caso esteja apenas testando a aplicação rode as seeds do projeto:

php artisan db:seed

